doctype html
html(lang='en')
  include u_header.jade
  body
   
    include u_navbar.jade
    
    // Page Header
    // Set your background image for this header on the line below.
    header.intro-header(style="background-image: url('img/about-bg.jpg')")
      .container
        .row
          .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
            .site-heading
              h1 #{name}
              hr.small
              span.subheading 一个有着梦想的码农.
    

    // Main Content
    .container
      .row
        .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1#postlist
          each post in posts
            .post-preview
              a(href='/artical?id=' + post._id)
                h2.post-title
                  | #{post.title}
                h3.post-subtitle
                  | #{post.subtitle}
              p.post-meta
                | Posted by 
                a(href='/user?name=' + post.name) #{post.name}
                |  on #{post.time.minute}
            hr
        
      
    include u_footer.jade
    